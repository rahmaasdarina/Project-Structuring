package repositories

import (
	"projectstructuring/models"
)

// func RepoCar(c *fiber.Ctx) error {
// 	return c.SendString("Masuk repo")
// }

func RepoCar() (result []models.Car, err error) {
	return result, nil
}
